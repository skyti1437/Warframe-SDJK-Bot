# 运行产物：群配置 / 推送订阅 / 渲染缓存 —— 含使用者数据，绝不入库
runtime/

# 运行时生成的数据快照（跑了才有）
core/data/wm_ranks.json
core/data/riven_weekly.json
core/data/de/wiki_disp.json

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# 测试临时产物（测试自己会清，失败中断时可能残留）
tests/_tmp_*

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store
Thumbs.db
