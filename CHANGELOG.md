# 更新日志

本文件记录 AstrBot 插件 **Warframe SDJKBOT** 的主要版本更新，最新版本排在最前。

> 这份文件同时被两处读取：AstrBot 面板里插件的「更新日志」页，以及 AstrBot
> 插件市场详情页的「更新日志」Tab（提交发布包时由平台解析入库）。
> 因此**每次发版都要在这里补一条**，否则市场那一片是空的。

## v1.0.8

**用户字体落点合规、FlareSolverr 告警分级与黑话大补。**

- **修复：用户自带字体不再随更新丢失** —— 启动时把插件目录里**非随包**的字体
  （`core/data/fonts/` 下的 ttc/ttf/otf，除随包子集外）一次性搬进插件数据目录
  `data/plugin_data/astrbot_plugin_warframe/fonts/`（渲染查找链已含该目录，
  插件更新/重装都不会删；随包子集与开发/克隆树不动）。`scripts/fetch_font.py`
  的下载落点同步改到该目录（自动识别部署布局，支持 `--data-dir`）。
- **新增：可选组件告警分级** —— 不再对不用 FlareSolverr 的用户每小时刷 WARN：
  配置关闭 → 完全不尝试且不告警；开启但**检测不到可用的 FS** → 跳过快照刷新并
  **每 24h 只提示一次**（附「关闭开关 / 部署 FS」两条处置路径）；FS 可达但
  **求解失败/超时** → 照旧每轮 WARN（真故障不静默）。
- **新增：wiki 黑话/俗称检索大补** —— 战甲单字黑话 25 键（猫/电/沙/鸟…）与
  用户《战甲黑话》清单 86 键（含错位映射修正）；**战甲外号页 60 条**（落国际服
  英文页面）；易混淆澄清 34 条；概念页分支也出卡；模糊匹配阈值 0.6→0.8，
  短词不再被新增「X+P」键劫持。
- **新增：社区快照兜底（不部署 FlareSolverr 也能拿到较新轮换数据）** ——
  元素加成与言录使货单只在 wiki 有（DE 官方接口实测不下发），此前没 FS 的用户
  只能吃随包种子。现在服务器上已抓到的新鲜快照会发布到公开仓的 `bot-data` 分支
  （只读 JSON，`raw.githubusercontent.com` 免盾），插件在「本地过期 + FS 不可用」时
  自动采用；**面板关闭 FlareSolverr 或 FS 不可达时也会走这条兜底**（一次普通 GET，与 FS 无关，
  实测告警分级的三态分流里原本会整段跳过刷新——那样最需要它的用户反而拿不到）；
  **三项校验不过一律不采用**（解析残缺 / 已过期 / 比本地旧），仍退回包内种子。
  发布侧同规矩：残缺、过期、相位与填充不符的数据一律不发。数据权威来源仍是 wiki。
- **修复：加载失败时的报错不再被"兜底"掩盖** —— `except ImportError` 的绝对导入
  兼容分支只在**非包上下文**（离线脚本）才回退；包内导入失败一律原样抛出。
  （2026-09-25 实证：从市场更新时新 `main.py` 已落地、`core/render.py` 还是旧的，
  真错误「render 少一个函数」被换成 `No module named 'core'`，面板/日志第一眼全被误导。）
- **改进：后台检查点对齐 `XX:05 UTC`（±0–15 分钟抖动）** —— 换轮都在 00:00 UTC
  （效价每 4 天、言录使每周），旧的 `sleep(6h)` 让检查点跟着开机时刻漂，最坏要
  等 6 小时才发现新轮次；对齐后最坏约 5 分钟，抖动避免所有实例扎堆打 wiki。
- **改进：社区快照独立 30 分钟轻轮询**（只对没装/连不上 FlareSolverr 的用户生效）——
  原先三态分流对他们是**整段跳过**，等于最需要社区快照的人反而拿不到。
- **修复：`wm` 三处"解析了却没接线"的过滤（帮助文案承诺过、实际静默不生效）** ——
  **品级**（`满级`：原来写死 10 级，而赋能满级是 **5**、川流不息也是 5 → 这类查询必然空表；
  现在按**物品实际满级**取，来源三级降级：`items.maxRank` → 挂单里出现的最高级（卡面注明）
  → 类别兜底；挂单完全没有级数时忽略该词并说明）、**遗物精炼档**（完整/优良/无瑕/光辉，
  按订单 subtype 过滤并逐行标出档位）、**墨染**（Atragraph/Foil Mod，按 subtype 过滤 + 行内标注）。
  另外补上精炼档的**官方写法「无瑕」**（此前只认社区写法「无暇」，用户照帮助文案输入会不生效）。
- **修复：赋能两族命名规则**（灰机 API 实测）+ 半中半英效果行人工覆盖。
- **新增：知识库文档 #14《战甲俗称与招募黑话》**（随包 `kb/`）。
- **工程**：`ruff` 静态检查清零（修掉 2 处未定义名、4 处重复字典键、未用导入/变量
  等 60+ 处；配置随开源仓分发）；README 补「第三方来源与许可」（含算法参考项目
  **未附带许可证**的如实说明）；metadata 分组数修正 + 补 `short_desc`；
  AGENTS.md 增「迁移/删除类护栏必须双向验证」纪律。

## v1.0.7

**FlareSolverr 失败自愈与超时放宽。**

- **新增：随包分发子集中文字体（开箱出图）** —— 市场 / 开源版不再依赖系统中文字体：
  自带 Noto Sans CJK SC 子集（GB2312 全表 6763 字 + 语料符号 + ASCII，两档共约
  6.7 MB），装完即可渲染卡片；与原完整字库**逐像素一致**（回归测试 5 张卡 0 像素差）。
  完整字库仍可 `scripts/fetch_font.py` 按需下载；自放字体放
  `plugin_data/<插件>/fonts/`（插件更新/重装都不丢）。
- **新增：wiki 简介卡数据随包分发** —— `wiki_intro.json`（12666 张简介卡）与
  `wiki_effect_zh.json`（1997 条 MOD 效果汉化）**进市场/开源包**（压缩后 +0.94MB）：
  市场版从此也有完整简介卡与中文效果行，与自部署版一致；此前「只有自部署版带简介」
  的体积理由已不成立。数据文件缺失时仍自动退「最小卡 + 英文效果」（防御性兜底）。

- **修复：FlareSolverr 会话崩溃后不再连败** —— 持久会话 `sdjk` 的 Chromium 标签页
  崩溃（`tab crashed`）后，旧逻辑只发 `sessions.create`（对**已存在**会话是空操作），
  插件会拿着坏会话每小时重试、连败一整天；现改为 **destroy→create 重建**，且连接级
  失败 / 非 JSON 响应也走重建重试（2026-09-25 线上事故：08:18、09:18 两轮刷新失败）。
- **修复：失败原因不再被掩盖** —— 多候选地址时，较早候选的连接错误会顶替 FS 的真实
  报错（日志报「All connection attempts failed」，真因其实是 `tab crashed`）；
  现以「收到的响应」里的错误信息为准。
- **调参：`maxTimeout` 60000 → 120000** —— CF 解页实测约 61 秒，60 秒会让求解整轮
  卡在超时边缘。
- **新增：每轮快照刷新前回收会话** —— 主动 destroy→create，让每轮都从全新标签页开始，
  防 Chromium 长跑崩（代价 = 该轮首解走冷启动，已有重建重试兜底）。
- 新增回归测试 `tests/test_flare_session_rebuild.py`（13 条断言）。
- **基线刷新**：终幕 B 批换轮（09-25 08:00）后元素重掷，`rotations.json` 与
  `_SPOT` 抽查值同步至 wiki 当前值（B 批 7 把全量更新）。

## v1.0.6

**武器名变体解析与倾向数据源重构；赏金小帐篷与 wiki 卡片增强。**

- **新增：赏金小帐篷行（issue #1）** —— 「赏金 地球」详情卡末尾新增「小帐篷 A/B/C」
  三行，直接列出平原三处营地当前挂的赏金（每处 3 条），阿耶车不用逐个帐篷进游戏看。
  DE 接口不下发帐篷归属，由世界种子 + 游戏任务清单按游戏同款算法推算，
  与实测截图及独立实现对拍一致（当前覆盖地球平原；金星 / 火卫二点位已验证，待官方中文名）。
- **新增：wiki 查询链接可点击（issue #1）** —— 命中词条直接发文本链接；
  有简介数据的词条附简介图卡（含变体说明与行内极性图标），链接仍以文本另发保证可点；
  `-1` 强制纯文本、`-t` 强制图片。页面名按国际服口径归一（战甲名不翻译；
  纯中文名去空格，修「玻之武杖 Prime」类死链），概念页兜底（奸商、新手等直达正确页面）。
- **新增：材料 / 配方 / 装饰资料卡** —— 生物质等全图资源有掉落兜底卡；
  配方用途三级兜底（修复「电路」脏行）；外观装饰 9173 条入卡（雕塑类带图）；
  占位符读顺处理（如 〈DAMAGE〉）。
- **新增：MOD 效果整句汉化 1997 条** —— 英文效果原文整句翻译（术语表统一），
  效果行不再截断；以现有译文表为底增量合并，重跑不抹旧条目。
- **新增：遗物中文黑话部件查询** —— 「遗物 水晶p 蓝图」可直接反查
  Citrine Prime 蓝图的遗物出处（`p` / `prime` 后缀均支持，连写亦可）；
  遗物索引补可复现构建器并重建至当前轮次（遗物 768 → 799、部件 595 → 607，
  含 Citrine Prime 四张与先锋 / 安魂纪元），部件名统一官方带空格形态。
- **新增：黑话 水晶甲 / 水晶 / 水晶p → Citrine Prime**（新 Prime 甲）。
- **修复：紫卡 / 玩家实测报障两轮共 9 项** —— 负词条符号、武器名兜底、
  幅度位方向、倾向汉化与曲翼分类、wr 排序、老卡反推；`p` 后缀区分
  基础版 / Prime（含 7 族武器）、家族倾向剥后缀、识图丢小数点修正。
- **新增：武器名变体解析** —— `倾向`、`wr`（紫卡）、`xh`（玄骸）、`伤害` 全面支持
  变体写法：`绝路p`、`赤毒沙皇`、`沙皇赤毒`、`kuva 布拉玛` 等缩写与前后语序均可命中；
  官方名优先级不变，查变体不会再错给本体数据。
- **新增：本体名查询列出全部变体** —— `倾向 绝路` 会同时列出 绝路（0.95）与
  绝路 Prime（0.70），各带倾向值与武器类别。
- **新增：截短缩写解析** —— `守望p` 直接命中 守望者 Prime。
- **修复：倾向数据源切换至 wiki 权威表（628 条）** —— 此前部分 Prime/变体武器
  不在市场倾向表里，会错给本体值（如 绝路p 曾显示 0.95，实际 0.70；
  守望者p 曾显示 0.90，实际 1.00）。现倾向表 418 → 649 条，并随平衡补丁刷新。
- **修复：倾向卡武器类别中文化**（步枪 / 霰弹枪 / 手枪 / 近战 / Archwing 系）。
- **修复：Prisma 系官方简中「棱晶」解析**（棱晶/棱镜 两种写法均可）。
- **掉落表刷新至「Iceblade of Narin」** —— 新增 Citrine Prime 四张遗物
  （Lith S19 / Meso C11 / Neo C11 / Axi C12）与扎里曼新赏金节点的中文掉落信息；
  信条效价基线同步刷新至当前轮次。
- **修复：`wiki` 查询改为纯文本直发** —— 链接可直接点击（此前渲染成图片无法点开）。
- **修复：wiki 黑话别名纠错** —— 「音妈」「DJ」等黑话落到官方简中页面名（恸哭女妖 /
  吟游歌者）；修正「充沛」（实为赋能·充沛）与「蛇发女妖」（实为武器）两条错映射；
  再修「生机」（实为赋能·生机 Arcane Pulse）与「瞬时狡诈」（实为弹指瞬技
  Fleeting Expertise）两条错映射；新增别名子串审计脚本，防同类「键=官方名尾段」错映射复发。

## v1.0.5

**市场包修复与开源层补齐。**

- 修正发布包内 README 的版本标题（此前误写为 v1.0.3，导致插件页显示旧版本号）。
- 补齐开源层的运维工具脚本（scripts/ops、scripts/kb 共 8 个），与公开仓库完全一致。
- gitleaks 工作流的安装步骤增加重试，避免临时网络故障造成 CI 误报失败。

## v1.0.4

**识卡提速、等级更准。**

- **识卡大图自动缩小**：识卡走 provider **直连**（不经过 AstrBot 的图片预处理与压缩），
  传给模型的就是原图，而视觉模型的推理耗时随像素近似线性 —— 生产实测 2326×870
  要 39 秒（同一模型在实验室把图压到 134 KB 时只要 6.65 秒）。此前只有「过小放大」
  没有「过大缩小」，现在提交识图前会把宽度规整到 **1600 px** 以内，预计省下约一半
  等待时间；MOD 名与容量数字在 1600 宽下仍清晰。失败时原样回落到原图，不影响识别。
- **识卡等级改用「豆子」交叉印证，0 级卡不再被判成满级**：卡片的容量数字常有
  **多重解释** —— 例如「容量 5」对私法补给（底 4 / 满 5）同时满足 1 级白、5 级绿、
  0 级红三种情况，此前按「满级假设」取最高，会把 **0 级的卡判成满级**；而改成
  只看数字颜色也不行 —— 视觉模型会把**极性图标**的红色误当成数字颜色。
  现在改为识别卡片底部的**「豆子」（菱形等级刻度）**：**点亮的豆子数就是当前等级**，
  与容量反推互相印证后才定级（两条路径独立：一个读数字、一个数像素，一致才敢用）。
  实测暮斩整张配卡 **9/9 与真值一致**，其中容量反推会判错的「北风 1 级」
  「长时苦难 0 级」均由豆子纠正。检测在本地完成（不额外调用模型、不额外花钱），
  任何失败都自动回落到原来的策略。仍保留 `?` 标注 —— 截图过小或模型没把卡片读全时，
  照旧标存疑而不是硬猜。
- **容量读成 0 时不再假装正常**：个别 MOD 的容量被读成 0 时原先显示 `容量0`，
  看起来像这张卡只占 0 容量。现在「读到 0 **且**等级反推不出来」标成 `容量0?`
  （库里只有 2 张卡的基础容量真是 0 —— survival instinct / transfusion，
  它们能正常反推出等级，显示不受影响）。
  等级未定的 MOD 其效果按满级计入，伤害计算不会出错。
- **武器名识别修复**：标题栏形如 `+ 升级／暮斩 [30] 3`（全角 ／、段位、还有右侧的
  **极化次数**）。此前只切半角斜杠、也没剥尾部数字 → 名字残成「暮斩 3」→ 查不到武器，
  卡面显示「未能识别」。现在全角/半角分隔符、`[30]`（含 `【30】`）、尾部极化次数
  都能正确剥离；`MK1-布莱顿` 这类名字里本来就有的数字不受影响。
- **MOD 库元数据修正**：「仅识别」表的 `base_drain` / `max_rank` 此前**从未被校准**
  （校准脚本只处理「可算」表，而且脚本本身还缺一个导入、一跑就报错）。全量对拍后发现
  14 条 max_rank 错，其中包括 **剑风 5→3**（满级卡被显示成「3/5★非满级」）、
  鹰眼/隼目/稳定枪手/能量导引 5→3。已扩展脚本同时校准该表并修正数据。
- **姿态卡不再让整张配卡的对齐失效**：姿态卡（如「狂风压境」）没有可用于校验的
  容量候选集，旧逻辑要求它必须落在「整行豆数全 0」的格子上 → **整张图无解** →
  豆子信号全部作废，于是「北风 1 级」被判成 3 级、「长时苦难 0 级」被判成 4 级。
  现在这类卡按**通配**处理，并加了「专用槽位的卡应与普通卡分行」的破平判据。
- **卡面补上极性说明**：等级定下来后，由（基础容量、等级、卡面容量）**算术反查**
  分支，卡面会写明「极性不合（容量 +25%）」或「槽位极性匹配（容量减半）」
  —— 例如「北风」的 9 就是 6+1 之后 **+25%** 的结果。不采信模型读的颜色
  （实测它常读错：北风实际红 9 报成白、肢解实际绿 5 报成白）。
- **新增「满级线」第三信号**：满级卡片底部有一条**横贯全卡**的亮线，非满级没有
  （实测满级卡「有墨列占比」0.78~0.91，非满级 ≤0.43）。用它兜住「豆子被那条线
  吃掉一颗」的少数情况（实测「一击必杀」满级 5 曾被数成 4）。
- **豆子检测抗噪加固**：左侧属性面板的进度条、状态/连击徽章会被误当成卡片段，
  把列网格的锚点带偏（实测网格整体左移 84 px → 整行豆数全错）。现在用卡片宽度基准
  剔掉这类杂散段、并用**锚点投票**确定网格；整行都没有满级线（全是窄豆簇）时
  也有兜底，不会丢掉整行。
- **4K 截图不再算错等级**：豆子检测的判据原本是 1920×1080 下量出来的，4K（3840 宽）
  会让它们整体失效 —— 实测豆数从 `[5,7,3,1]` 变成 `[13,7,8,1]`（细线在 4K 下也变厚、
  被当成豆子）→ 对齐无解 → 豆子信号全废，于是「北风 1 级」又被判成 3 级。
  现在检测前会先量出卡片宽、把工作图**归一到基准尺度**（卡片宽 152 px），
  并把计数判据全部改成**相对卡片宽**（豆/线分界、回填门槛、计数上限），
  实测 **1920 / 2560 / 3840 / 1280 四种分辨率全部命中真值**。
  另外会在「检测到了但对齐无解」时把原图存到运行目录的 `scan_debug/`，
  便于事后定位（正常识卡不写盘）。
- **豆子颜色：实机核对后收敛为「只有蓝」**：早前据卡片素材推测「橙系边框（执刑官那类）
  的豆子是琥珀色」，于是给蓝色判据加了一层暖色兜底。后来用 8 张实机截图**逐颗采样**
  51 个格子，**全部是蓝色系**（含金框/稀有卡）—— 那个「琥珀」其实来自卡片样板上的
  装饰与文字。暖色兜底因此收紧为**只在整图蓝色判据完全扫不出装备区时**才启用
  （它曾经把金框卡的卡面美术当成豆子，导致整张图的等级判定作废）。
- **浅色/奥罗金主题无需额外处理**（实测 + 已加回归测试）：豆子判据要求「蓝通道显著
  高于红通道」，白底、浅色卡面这类低饱和背景天然不满足，因此浅色主题下不会出现
  「白底倒灌淹没豆子」的问题 —— 四种背景/卡面组合（深色、浅白+深卡、浅白+浅卡、
  浅白+灰卡）实测豆数全部正确。
- **裂罅紫卡单独归类**：紫卡名是「武器名 + 随机词缀」（如「野猪 Visi-satidex」），
  永远无法与静态词库全字匹配 —— 以前会被显示成「⚠ 库中未收录，已忽略」，看起来像
  在报我们缺数据。现在单独识别：卡面写明「裂罅紫卡（词缀随机，数值无法核算）」，
  并**照常按豆子报出它的等级**，不再混进「未收录」清单。
- **不再「只读到一排」**：视觉模型偶尔会漏读整排卡片（实测某次只读了上排 3 张、
  漏掉下排 4 张，卡面就只显示 3 张卡）。现在从两侧夹：
  ① 识别提示词明确「已装备区是界面中间那块网格的**全部卡片，通常两排，两排都要列**」；
  ② **漏读交叉校验** —— 像素网格里「有豆的格数」是这张图**至少有多少张卡**的硬下界
  （有豆 ⇒ 等级 > 0 ⇒ 该格必然装了卡）。模型读到的卡数低于下界就判为漏读并加罚，
  于是「漏读一半但面板校验碰巧少错 1 项」的结果不会再被选中。
- **单格检测噪声不再让整张图的豆子信号作废**：金框卡的**卡面美术本身就是暖色**，
  暖色兜底会把整片卡面点亮 —— 实测某张 0 级卡那格被判成 2 颗豆（真值 0），
  于是「豆数 ∈ 容量候选集」的约束不满足 → 整个对齐作废 → 7 张卡全部退回容量反推
  （用户看到的「等级又对不上」）。两处修：暖色兜底**只在整排都没有蓝豆时**才启用
  （一排已有蓝豆就说明它是普通卡排，绝不会凭空造豆）；对齐从「全有或全无」改为
  **最优拟合**（个别格不满足仍可采用，只让那一张卡退回容量反推），并且**该格豆数
  不在候选集时直接拒收**、把原因写在卡面上，不再静默。
- **MOD 库补入「稀有度」**（来自 wiki `Module:Mods/data` 的 `Rarity`，共 1012 条），
  供卡面边框/稀有度使用。同时新增 `scripts/audit_mods_vs_wiki.py` 可随时全量对拍。
  ⚠️ 注意：**该模块的 `BaseDrain` 不能用于校准** —— 用配卡截图裁决过
  （分裂膛室卡面 15 + 亮豆 5 颗 → 只有「基础 10」成立，模块写 4 会得 9），
  另有 6 条 `MaxRank` 模块写错（DE 自己的导出与 wiki 正文的等级表都与我们一致），
  已在脚本里列为永不应用，并加数据不变量测试钉住。

- **终幕（Coda）效价卡面修复**：官方武器名带 `Dual ` 前缀（如 `Dual Coda Torxica`），
  此前解析正则匹配不到 → 效价刷新整轮中止、A 批 7 把武器的**元素/加成全部缺失**。
  已修解析并补全数据；卡片现在每行都带「火焰 27.4%」式加成，刷新链路实测可正常写入。
- **裸「蹲」出卡片图**：裸「蹲」（及「蹲 帮助」）此前被写死为纯文本输出，
  现在与其余指令一致走卡片渲染（28 行「可蹲类型」帮助卡）；渲染失败仍自动回落文字。
- **言录使（Acrithis）本周货单自动化**：DE 不下发每周实际 5 件，此前快照过期后
  只在日志里告警等人工、卡面长期显示「货单待更新」。现在改为过期后**自动抓取**
  wiki《Acrithis/Current Offerings》当期上报子页（社区人工维护），校验通过自动落盘；
  社区尚未更新或抓取失败时才回落候选池展示。本周货单同步更新：
  赤毒 ×5000（10 苦栓）、主要武器赋能槽连接器（25）、奥罗金催化剂蓝图（20）、
  Forma 蓝图（10）、奥罗金反应堆蓝图（20）。
- **道具译名按官方表修正**：Primary/Secondary Arcane Adapter =
  **主要/次要武器赋能槽连接器**（此前误作「主/副武器」）。

## v1.0.3

**订阅推送修复、指令空间、市价查询（wm）解析修复、品牌更名。**

- **修复「蹲」订阅推送的静默失效**（本次最实质的 bug）：
  - `蹲 仲裁` 此前**永远不会推送** —— 旧数据源已停摆，采集侧抛错却被静默吞掉。
    现改为按社区排期表推算场次，筛选（高效 / 传奇 / 任务类型）真正生效。
  - `蹲 钢路侵袭` 同样走了已失效的数据源，改用当前可用的侵袭数据。
  - 非裂隙类型的筛选词此前被整体忽略（写了「高效」等于没写），现在生效；
    未评级场次不会被「高效」误命中。
  - 「警报」在官方数据里恒为空（该玩法已停用），不再作为可订阅类型误导用户。
- **品牌更名为 Warframe SDJKBOT**（原 Warframe SDJK）。插件 ID / 安装目录 /
  模块名一律不变，已安装用户无需重装，只是显示名与卡片文案变化。
- **修复与内置指令、其他插件抢答的问题**：AstrBot 会在唤醒阶段把 `/` 前缀剥掉，
  导致本插件实际也会响应 `/help`、`/新闻` 这类**属于别人**的指令，并且因为处理
  完会中断事件，把后面的处理器整体跳过（表现为「内置帮助打不开」「资讯插件的
  指令没反应」）。
  现在带前缀且该指令词**已被内置指令或其他插件占用**时会主动让路，且不中断事件；
  `/仲裁`、`/赏金` 这类没被占用的写法照常可用，裸词指令完全不受影响。
- 帮助卡新增一行说明：指令可直接发送、无需 `/` 前缀。
- **修复市价查询（wm）的中文名解析错误**（照官方名输入却查到别的东西）：
  - `wm 压迫点` / `压迫点 p` 此前返回**「膛线」**——官方简中「压迫点」是 Pressure
    Point，而词典把它错配到了 Serration。原因是别名词典（含双向包含的模糊匹配）
    排在官方名之前，官方名被劫持；现在**官方简中 / 英文 / slug 永远优先**。
    同类错误一并修正：空尖弹、精算蓄能、腐坏打击、金属纤维、锯齿弹链、鹰眼、
    加速充能。
  - `wm 毁灭Gp` / `毁灭Gprime` 此前返回**非 Prime** 的「毁灭 Grineer」（订单数与
    价格全不对）。原因是名称归一化把 `Primed` 里的 `prime` 当子串抹掉，
    连带把你写的 Prime 语义一起吃掉；现在 Prime 只在独立成词时才忽略，
    并按「你有没有写 Prime」决定查哪一版。
  - 词典整理：修正 8 条指向错误 slug 的黑话（守望者 / 绝路 / 布莱顿 / 拉特昂 …）；
    「加速」让回官方名（Nova 请用「诺娃」「加速娃」）；补充「捷德」（Jade）；
    移除若干来源不明的黑话。新增 `scripts/audit_wm_aliases.py` 可随时自查词典。
- **修复插件内置自动刷新的空转**：信条 / 终幕的元素加成快照此前**从来没真正刷新过**
  —— 新鲜度判定只要遇到第一个「仍在窗口内」的段就直接返回，而两段的换轮锚点相差
  24 小时，任何时刻都必然有一段新鲜，于是永远不抓取；另外终幕的批次基准存的是
  「当前批下标」而非相位基准，换轮后会整体错位一批。现在两段都过期才判定需要刷新，
  锚点由观测批反解，并补了回归测试。
- **言录使（Acrithis）周常周期修正**：此前周期被写成 24 小时（当成每日），到期时间也不对。
  正确口径是**每周一 00:00 UTC** 轮换 5 件（wiki《Acrithis》实时页面与《Reset》总表；
  Update 32.3 起各商人的周常已统一到周一），与其它周常同点。
  现在时刻一律按 UTC 判定，卡面换算成**北京时间**展示；货单过期时会明确标注
  「货单待更新」，不再静默把候选池当成实际在卖的 5 件。
  注：DE 不下发本周实际卖哪 5 件（`AcrithisVendorManifest` 只有候选池与权重），
  该数据仍是社区维护快照。

## v1.0.2

- 通过 AstrBot 插件市场发布（插件 ID `skyti1437/astrbot_plugin_warframe`）。
- 按平台规范整改：日志统一走 `astrbot.api.logger`；用户数据与快照统一写入
  `data/plugin_data/<插件名>/`（不再写插件包目录），并带旧数据一次性迁移。

## v1.0.1

- 修复 `wm` 部件关键词检索（`part` 字段解析），避免「wm 母牛 蓝图」搜出整套。
- 补漏若干官方简中译名（如 Inaros 之沙蓝图）。

## v1.0.0

- 版本号重置为 1.0，品牌统一。
- 玄骸查询重做：三类武器 + 元素 / 在线 / 信用筛选分支。
- 奸商新增下期预测；修复失效管理指令并做了一轮安全加固。
