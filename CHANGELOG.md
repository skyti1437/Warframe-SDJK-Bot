# 更新日志

本文件记录 AstrBot 插件 **Warframe SDJKBOT** 的主要版本更新，最新版本排在最前。

> 这份文件同时被两处读取：AstrBot 面板里插件的「更新日志」页，以及 AstrBot
> 插件市场详情页的「更新日志」Tab（提交发布包时由平台解析入库）。
> 因此**每次发版都要在这里补一条**，否则市场那一片是空的。

## v1.0.4

**识卡提速。**

- **识卡大图自动缩小**：识卡走 provider **直连**（不经过 AstrBot 的图片预处理与压缩），
  传给模型的就是原图，而视觉模型的推理耗时随像素近似线性 —— 生产实测 2326×870
  要 39 秒（同一模型在实验室把图压到 134 KB 时只要 6.65 秒）。此前只有「过小放大」
  没有「过大缩小」，现在提交识图前会把宽度规整到 **1600 px** 以内，预计省下约一半
  等待时间；MOD 名与容量数字在 1600 宽下仍清晰。失败时原样回落到原图，不影响识别。

## v1.0.3

**订阅推送修复、指令空间、市价查询（wm）解析修复、品牌更名。**

- **修复「蹲」订阅推送的静默失效**（本次最实质的 bug）：
  - `蹲 仲裁` 此前**永远不会推送** —— 旧数据源已停摆，采集侧抛错却被静默吞掉。
    现改为按社区排期表推算场次，筛选（高效 / 传奇 / 任务类型）真正生效。
  - `蹲 钢路侵袭` 同样走了已失效的数据源，改用当前可用的侵袭数据。
  - 非裂隙类型的筛选词此前被整体忽略（写了「高效」等于没写），现在生效；
    未评级场次不会被「高效」误命中。
  - 「警报」在官方数据里恒为空（该玩法已停用），不再作为可订阅类型误导用户。
- **品牌更名为 Warframe SDJKBOT**（原 Warframe SDJK）。插件 ID / 安装目录 /
  模块名一律不变，已安装用户无需重装，只是显示名与卡片文案变化。
- **修复与内置指令、其他插件抢答的问题**：AstrBot 会在唤醒阶段把 `/` 前缀剥掉，
  导致本插件实际也会响应 `/help`、`/新闻` 这类**属于别人**的指令，并且因为处理
  完会中断事件，把后面的处理器整体跳过（表现为「内置帮助打不开」「资讯插件的
  指令没反应」）。
  现在带前缀且该指令词**已被内置指令或其他插件占用**时会主动让路，且不中断事件；
  `/仲裁`、`/赏金` 这类没被占用的写法照常可用，裸词指令完全不受影响。
- 帮助卡新增一行说明：指令可直接发送、无需 `/` 前缀。
- **修复市价查询（wm）的中文名解析错误**（照官方名输入却查到别的东西）：
  - `wm 压迫点` / `压迫点 p` 此前返回**「膛线」**——官方简中「压迫点」是 Pressure
    Point，而词典把它错配到了 Serration。原因是别名词典（含双向包含的模糊匹配）
    排在官方名之前，官方名被劫持；现在**官方简中 / 英文 / slug 永远优先**。
    同类错误一并修正：空尖弹、精算蓄能、腐坏打击、金属纤维、锯齿弹链、鹰眼、
    加速充能。
  - `wm 毁灭Gp` / `毁灭Gprime` 此前返回**非 Prime** 的「毁灭 Grineer」（订单数与
    价格全不对）。原因是名称归一化把 `Primed` 里的 `prime` 当子串抹掉，
    连带把你写的 Prime 语义一起吃掉；现在 Prime 只在独立成词时才忽略，
    并按「你有没有写 Prime」决定查哪一版。
  - 词典整理：修正 8 条指向错误 slug 的黑话（守望者 / 绝路 / 布莱顿 / 拉特昂 …）；
    「加速」让回官方名（Nova 请用「诺娃」「加速娃」）；补充「捷德」（Jade）；
    移除若干来源不明的黑话。新增 `scripts/audit_wm_aliases.py` 可随时自查词典。
- **修复插件内置自动刷新的空转**：信条 / 终幕的元素加成快照此前**从来没真正刷新过**
  —— 新鲜度判定只要遇到第一个「仍在窗口内」的段就直接返回，而两段的换轮锚点相差
  24 小时，任何时刻都必然有一段新鲜，于是永远不抓取；另外终幕的批次基准存的是
  「当前批下标」而非相位基准，换轮后会整体错位一批。现在两段都过期才判定需要刷新，
  锚点由观测批反解，并补了回归测试。
- **言录使（Acrithis）周常周期修正**：此前周期被写成 24 小时（当成每日），到期时间也不对。
  正确口径是**每周一 00:00 UTC** 轮换 5 件（wiki《Acrithis》实时页面与《Reset》总表；
  Update 32.3 起各商人的周常已统一到周一），与其它周常同点。
  现在时刻一律按 UTC 判定，卡面换算成**北京时间**展示；货单过期时会明确标注
  「货单待更新」，不再静默把候选池当成实际在卖的 5 件。
  注：DE 不下发本周实际卖哪 5 件（`AcrithisVendorManifest` 只有候选池与权重），
  该数据仍是社区维护快照。

## v1.0.2

- 通过 AstrBot 插件市场发布（插件 ID `skyti1437/astrbot_plugin_warframe`）。
- 按平台规范整改：日志统一走 `astrbot.api.logger`；用户数据与快照统一写入
  `data/plugin_data/<插件名>/`（不再写插件包目录），并带旧数据一次性迁移。

## v1.0.1

- 修复 `wm` 部件关键词检索（`part` 字段解析），避免「wm 母牛 蓝图」搜出整套。
- 补漏若干官方简中译名（如 Inaros 之沙蓝图）。

## v1.0.0

- 版本号重置为 1.0，品牌统一。
- 玄骸查询重做：三类武器 + 元素 / 在线 / 信用筛选分支。
- 奸商新增下期预测；修复失效管理指令并做了一轮安全加固。
