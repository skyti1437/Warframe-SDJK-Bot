# 统一按 LF 存储/检出。
# 少了这一条，Windows 上 Git 默认 autocrlf=true 会把检出写成 CRLF，
# 而构建脚本每次从源仓库复制出来的是混合行尾 —— git status 会显示一堆
# 假的 M（内容其实没变），提交历史里全是行尾噪音（2026-09-18 实测）。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.ttc binary
*.png binary
*.zip binary
*.jpg binary
